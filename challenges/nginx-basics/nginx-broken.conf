user www-data;
worker_processes 1   # <- BROKEN: missing semicolon

events {
    worker_connections 1024;
}

http {
    server {
        listen 8080;   # <- BROKEN: should be port 80
        server_name localhost;

        location / {
            root /var/www/html;
            index index.html;
        }
    }
}
